use rand::Rng;
//use rand::Rng::*;将Rng模块下的所有公有项引入作用域
use std::collections::HashMap;
use std::{
    cmp::Ordering,
    io,
};
// use std::io::Write;
// use std::io;
fn main() {
    println!("Hello, world!");
}
